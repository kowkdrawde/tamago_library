<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>

  <body style="max-width:400px;font-family:Arial,Helvetica,sans-serif;">
    <% attachments.inline["emailheader.jpg"] = File.read("app/assets/images/development/emailheader.jpg") %>
    <%= image_tag attachments['emailheader.jpg'].url, style: 'width:100%;height:auto' %>
    <h3>Hi <%= "#{@user.firstname} #{@user.lastname}" %>!</h3> 

	  <p>Someone has requested a link to change your password. You can do this by <%= link_to 'clicking here', edit_password_url(@resource, reset_password_token: @token) %> through the link below.</p>

	  <p>If you didn't request this, please ignore this email.</p>
	  <p>Your password won't change until you access the link above and create a new one.</p>

    <div style="font-style:italic;"><%= render 'user_mailer/reminder' %></div>

  </body>

</html>
